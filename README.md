# Project Indexer - MCP Server

Интеллектуальный MCP (Model Context Protocol) сервер для индексации и семантического поиска по кодовым проектам с использованием ChromaDB и OpenAI.

## Возможности

- 🧠 **Контекстный анализ проекта**: Автоматическое понимание стека, архитектуры и назначения проекта перед индексацией файлов
- 🔍 **Семантический поиск**: Поиск кода по смыслу, а не по ключевым словам
- 📦 **Векторное хранилище**: ChromaDB для быстрого и точного поиска
- 🤖 **AI-анализ**: OpenAI GPT-4o-mini для анализа кода и text-embedding-3-small для embeddings
- 🚀 **Параллельная обработка**: До 5 файлов одновременно
- 📝 **Умная разбивка**: Автоматическое разбиение больших файлов на чанки

## Как это работает

### Процесс индексации

1. **Анализ контекста проекта** (~10-30 секунд):
   - Сканирует структуру проекта (до 3 уровней)
   - Читает README, package.json, pyproject.toml и другие конфигурационные файлы
   - Определяет tech stack, фреймворки, архитектуру
   - Отправляет в OpenAI для понимания общего контекста

2. **Сканирование файлов**:
   - Обход дерева директорий с учетом .gitignore
   - Фильтрация по паттернам include/exclude

3. **Обработка каждого файла**:
   - Разбивка на чанки (если файл большой)
   - Анализ через OpenAI **с контекстом проекта**
   - Генерация embeddings (код + описание + контекст)

4. **Хранение в ChromaDB**:
   - Сохранение векторных представлений
   - Метаданные для фильтрации

### Преимущество контекста

**Без контекста:**
```
Анализ: "This file handles authentication"
```

**С контекстом:**
```
Проект: E-commerce API (Django/DRF)
Анализ: "JWT authentication for e-commerce API. Extends DRF's
         TokenAuthentication and integrates with User model for
         customer authentication in checkout flow."
```

## Установка

### 1. Клонировать и установить зависимости

```bash
cd project-scanner

# Используя uv
uv pip install -e .

# Или используя pip
pip install -e .
```

### 2. Настроить окружение

```bash
# Скопировать примеры конфигурации
cp .env.example .env
cp config.yaml.example config.yaml

# Отредактировать .env и добавить OpenAI API key
nano .env
```

В `.env`:
```bash
OPENAI_API_KEY=sk-your-api-key-here
```

## Использование

### Как MCP сервер (с Claude Desktop)

1. **Добавить в конфигурацию Claude Desktop**:

На macOS: `~/Library/Application Support/Claude/claude_desktop_config.json`

```json
{
  "mcpServers": {
    "project-indexer": {
      "command": "python",
      "args": ["-m", "src.server"],
      "env": {
        "OPENAI_API_KEY": "sk-your-key-here"
      },
      "cwd": "/полный/путь/к/project-scanner"
    }
  }
}
```

2. **Перезапустить Claude Desktop**

3. **Использовать инструменты в чате**:

```
Проиндексируй мой проект в /Users/me/my-project
```

Claude автоматически вызовет `index_project` tool.

```
Найди код для обработки аутентификации
```

Claude вызовет `search_code` tool.

### Доступные MCP инструменты

#### 1. `index_project`

Индексирует весь проект с AI анализом.

**Параметры:**
- `project_path` (required): Абсолютный путь к корню проекта
- `force_reindex`: Принудительная переиндексация (default: false)

**Пример:**
```python
index_project(project_path="/Users/me/my-project")
```

#### 2. `search_code`

Семантический поиск по коду.

**Параметры:**
- `project_path` (required): Путь к проекту
- `query` (required): Поисковый запрос
- `n_results`: Количество результатов (default: 5, max: 50)
- `file_type`: Фильтр по типу (code|documentation|config|test)
- `language`: Фильтр по языку
- `include_code`: Включать код в результаты (default: true)

**Пример:**
```python
search_code(
    project_path="/Users/me/my-project",
    query="функция для парсинга JSON",
    n_results=10
)
```

#### 3. `get_project_info`

Получить информацию об индексированном проекте.

**Параметры:**
- `project_path` (required): Путь к проекту

**Пример:**
```python
get_project_info(project_path="/Users/me/my-project")
```

#### 4. `delete_project_index`

Удалить индекс проекта.

**Параметры:**
- `project_path` (required): Путь к проекту
- `confirm` (required): Должно быть `true` для подтверждения

**Пример:**
```python
delete_project_index(
    project_path="/Users/me/my-project",
    confirm=true
)
```

## Конфигурация

### Переменные окружения (.env)

```bash
# OpenAI
OPENAI_API_KEY=sk-...            # REQUIRED
OPENAI_MODEL=gpt-4o-mini         # Модель для анализа
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# ChromaDB
CHROMA_PERSIST_DIRECTORY=./chroma_data  # Где хранить данные

# Индексация
MAX_FILE_SIZE_MB=1               # Максимальный размер файла
MAX_CHUNK_SIZE_TOKENS=6000       # Размер чанка
MAX_CONCURRENT_FILES=5           # Параллельная обработка
RATE_LIMIT_RPM=3500              # Лимит запросов в минуту
RATE_LIMIT_TPM=1000000           # Лимит токенов в минуту
```

### Паттерны файлов (config.yaml)

Настройте какие файлы индексировать:

```yaml
include_patterns:
  - "**/*.py"
  - "**/*.js"
  - "**/*.ts"
  # ... добавьте свои паттерны

exclude_patterns:
  - "**/node_modules/**"
  - "**/venv/**"
  # ... добавьте свои исключения
```

## Архитектура

```
src/
├── server.py                    # MCP сервер (точка входа)
├── config.py                    # Управление конфигурацией
├── indexer/
│   ├── context_analyzer.py      # Анализ контекста проекта
│   ├── scanner.py               # Сканирование файлов
│   ├── analyzer.py              # Анализ кода через OpenAI
│   ├── chunker.py               # Разбивка файлов
│   ├── embedder.py              # Генерация embeddings
│   └── index_manager.py         # Оркестратор индексации
├── storage/
│   ├── chroma_client.py         # ChromaDB обертка
│   └── models.py                # Модели данных
└── utils/
    ├── logger.py                # Логирование
    ├── rate_limiter.py          # Rate limiting
    └── file_types.py            # Утилиты
```

## Примеры использования

### Индексация проекта

```python
# Через MCP в Claude Desktop
"Проиндексируй проект /Users/me/my-fastapi-app"

# Результат:
{
  "status": "success",
  "project_context": {
    "project_name": "my-fastapi-app",
    "tech_stack": ["Python", "FastAPI"],
    "frameworks": ["FastAPI", "SQLAlchemy"],
    "architecture_type": "microservices"
  },
  "stats": {
    "total_files": 45,
    "indexed_files": 45,
    "total_chunks": 52,
    "duration_seconds": 127.3
  }
}
```

### Поиск кода

```python
# В Claude Desktop
"Найди где обрабатываются HTTP запросы"

# Или с фильтрами
"Найди тесты для аутентификации"
```

## Troubleshooting

### Ошибка: OPENAI_API_KEY не установлен

```bash
# Проверьте .env файл
cat .env | grep OPENAI_API_KEY

# Убедитесь что ключ правильный
```

### Ошибка: Rate limit exceeded

Уменьшите значения в `.env`:
```bash
MAX_CONCURRENT_FILES=3
RATE_LIMIT_RPM=1000
```

### ChromaDB ошибки

```bash
# Удалить и пересоздать базу данных
rm -rf ./chroma_data
```

## Производительность

### Типичная скорость индексации

- **Маленький проект** (50 файлов): ~2-3 минуты
- **Средний проект** (200 файлов): ~8-12 минут
- **Большой проект** (1000 файлов): ~45-60 минут

Зависит от:
- Размера файлов
- Количества чанков
- Лимитов OpenAI API
- Настройки `MAX_CONCURRENT_FILES`

## Стоимость

### OpenAI API расходы (примерно)

Для проекта из **200 файлов**:

- **Анализ контекста**: ~$0.01
- **Анализ файлов** (GPT-4o-mini): ~$0.50-1.00
- **Embeddings** (text-embedding-3-small): ~$0.10-0.20

**Итого**: ~$0.60-1.20 за индексацию

### Оптимизация стоимости

1. Используйте `exclude_patterns` для игнорирования ненужных файлов
2. Уменьшите `MAX_FILE_SIZE_MB` чтобы пропускать большие файлы
3. Индексируйте только измененные файлы (будет в будущих версиях)

## Лицензия

MIT

## Поддержка

Для вопросов и проблем создайте issue в репозитории.

## Roadmap

- [ ] Инкрементальная индексация (обновление только измененных файлов)
- [ ] File system watcher для автоматического обновления
- [ ] Поддержка нескольких проектов одновременно
- [ ] Web UI для управления индексами
- [ ] Кастомные модели embeddings
- [ ] Генерация call graph
- [ ] Экспорт результатов в различные форматы
