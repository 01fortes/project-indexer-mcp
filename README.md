# Project Indexer - MCP Server

Интеллектуальный MCP (Model Context Protocol) сервер для индексации и семантического поиска по кодовым проектам с использованием ChromaDB и OpenAI.

## ✨ Возможности

- 🧠 **Контекстный анализ проекта**: Автоматическое понимание стека, архитектуры и назначения проекта перед индексацией файлов
- 🔍 **Семантический поиск**: Поиск кода по смыслу, а не по ключевым словам
- 🕸️ **Call Graph Analysis (NEW!)**: Трассировка полных code flow от trigger до external API
  - Двухпроходная индексация (AST + LLM)
  - Детекция entry points (HTTP, Kafka, Scheduled tasks)
  - Классификация по слоям (trigger → controller → service → provider → external)
  - Граф вызовов через SQLite + семантический поиск через ChromaDB
- 📦 **Векторное хранилище**: ChromaDB для быстрого и точного поиска
- 🤖 **AI-анализ**: OpenAI GPT-5.2-Codex (Responses API) + text-embedding-3-small
- 🚀 **Параллельная обработка**: До 5 файлов одновременно
- 📝 **Умная разбивка**: Автоматическое разбиение больших файлов на чанки
- 🔄 **Инкрементальное обновление**: Обновление отдельных файлов без переиндексации проекта

## 🛠️ MCP Инструменты (8)

### Основные
1. **`index_project`** - Полная индексация проекта с AI-анализом
2. **`search_code`** - Семантический поиск с возвратом кода
3. **`search_files`** - Быстрый поиск файлов (только пути)
4. **`get_project_info`** - Информация о проекте

### Управление
5. **`list_projects`** - Список всех проиндексированных проектов
6. **`update_files`** - Обновить/добавить конкретные файлы
7. **`remove_files`** - Удалить файлы из индекса
8. **`delete_project_index`** - Удалить весь индекс проекта

📖 **Полное описание всех инструментов**: [API_REFERENCE.md](API_REFERENCE.md)

## 🎯 Как это работает

### Инновация: Context-First подход

#### 1. **Анализ контекста проекта** (~10-30 секунд)
Перед индексацией файлов система:
- Сканирует структуру проекта (до 3 уровней)
- Читает README, package.json, pyproject.toml, Cargo.toml и другие конфиги
- Определяет tech stack, фреймворки, архитектуру
- Отправляет в OpenAI GPT-5.2-Codex для понимания общего контекста
- Сохраняет контекст как специальный документ в ChromaDB

#### 2. **Индексация файлов с контекстом**
Каждый файл анализируется с использованием контекста проекта:

**Без контекста:**
```
"This file handles authentication"
```

**С контекстом (Context-First):**
```
Проект: E-commerce API (Django/DRF)
Файл: auth/login.py

Анализ: "JWT authentication для e-commerce API.
Extends DRF's TokenAuthentication и интегрируется
с User model для аутентификации покупателей в
checkout flow."
```

#### 3. **Векторное хранение**
- Embedding = код + AI описание + project context
- Сохранение в ChromaDB для быстрого поиска
- Метаданные для фильтрации (язык, тип файла, зависимости)

### Технологический стек

- **Python 3.12** (важно: не 3.14!)
- **MCP SDK** (Model Context Protocol)
- **OpenAI GPT-5.2-Codex** - анализ кода через Responses API
- **OpenAI text-embedding-3-small** - генерация векторов
- **ChromaDB** - векторная база данных
- **FastMCP** - фреймворк для MCP серверов

## 📦 Установка

### 1. Требования

- Python 3.12 (НЕ 3.14! ChromaDB не совместим)
- OpenAI API ключ

**Установка Python 3.12:**
```bash
brew install python@3.12
```

### 2. Клонировать репозиторий

```bash
cd /path/to/your/projects
git clone <repository-url>
cd project-scanner
```

### 3. Создать виртуальное окружение

```bash
# Используйте Python 3.12
/opt/homebrew/bin/python3.12 -m venv venv

# Активировать
source venv/bin/activate

# Проверить версию
python --version  # Должно быть 3.12.x
```

### 4. Установить зависимости

```bash
pip install -r requirements.txt
```

### 5. Настроить конфигурацию

```bash
# Скопировать пример
cp .env.example .env

# Отредактировать и добавить OpenAI API ключ
nano .env
```

В `.env` установите:
```bash
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-5.2-codex
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
```

### 6. Проверить установку

```bash
python test_imports.py
```

Должно вывести:
```
✅ All imports successful!
✅ Configuration is valid!
```

## 🚀 Использование

### С Claude Desktop (рекомендуется)

#### 1. Настроить Claude Desktop

Откройте конфигурацию:
```bash
nano ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

Добавьте:
```json
{
  "mcpServers": {
    "project-indexer": {
      "command": "/полный/путь/к/project-scanner/venv/bin/python",
      "args": ["/полный/путь/к/project-scanner/run_server.py"],
      "env": {
        "OPENAI_API_KEY": "sk-your-openai-api-key-here"
      }
    }
  }
}
```

**Важно:** Используйте ПОЛНЫЕ абсолютные пути!

#### 2. Перезапустить Claude Desktop

Полностью закройте и откройте заново.

#### 3. Проверить подключение

В чате спросите:
```
Какие MCP инструменты доступны?
```

Должно показать **project-indexer** с 8 инструментами.

### Примеры использования

#### Индексация проекта
```
Проиндексируй проект /Users/john/projects/my-app
```

#### Поиск кода
```
Найди где обрабатываются HTTP запросы
```

```
Покажи функции для работы с базой данных
```

#### Поиск только файлов
```
Найди файлы связанные с аутентификацией
```

#### Обновление файлов
```
Обнови файлы src/api.py и src/config.py
```

#### Удаление файлов
```
Удали из индекса файл old/deprecated.py
```

#### Список проектов
```
Покажи все проиндексированные проекты
```

## 📊 Стоимость использования

### С gpt-5.2-codex (рекомендуется)

Для проекта из **150 файлов**:
- Анализ контекста: ~$0.05-0.10
- Анализ файлов: ~$1.50-2.50
- Embeddings: ~$0.10-0.20
- **Итого**: ~$1.65-2.80

**Преимущества:**
- 🎯 Лучшая точность для кода
- 🧠 Reasoning capabilities
- 💰 40-80% экономия через кеширование
- ⚡ 3% лучше на SWE-bench

### Альтернатива: gpt-4o-mini

В ~10 раз дешевле (~$0.15-0.25), но:
- ❌ Хуже понимает код
- ❌ Менее точные описания
- ❌ Нет reasoning capabilities

**Рекомендация:** Используйте gpt-5.2-codex для продакшена.

## 🏗️ Архитектура

```
project-scanner/
├── src/
│   ├── server.py              # MCP сервер с 8 инструментами
│   ├── config.py              # Управление конфигурацией
│   ├── indexer/
│   │   ├── context_analyzer.py  # Анализ контекста проекта
│   │   ├── scanner.py           # Сканирование файлов
│   │   ├── analyzer.py          # Анализ через OpenAI (Responses API)
│   │   ├── chunker.py           # Разбивка больших файлов
│   │   ├── embedder.py          # Генерация embeddings
│   │   └── index_manager.py     # Оркестрация индексации
│   ├── storage/
│   │   ├── chroma_client.py     # ChromaDB wrapper
│   │   └── models.py            # Модели данных
│   └── utils/
│       ├── file_types.py        # Определение типов файлов
│       ├── logger.py            # Логирование
│       └── rate_limiter.py      # Rate limiting для OpenAI
├── run_server.py              # Точка входа
├── .env                       # Конфигурация (не в git)
└── README.md                  # Этот файл
```

## 🔧 Конфигурация

### Переменные окружения (.env)

```bash
# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-5.2-codex
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# ChromaDB
CHROMA_PERSIST_DIRECTORY=./chroma_data

# Индексация
MAX_FILE_SIZE_MB=1
MAX_CHUNK_SIZE_TOKENS=6000
CHUNK_OVERLAP_TOKENS=500
MAX_CONCURRENT_FILES=5

# Rate Limiting
RATE_LIMIT_RPM=3500
RATE_LIMIT_TPM=1000000
```

### Паттерны файлов (config.yaml)

По умолчанию индексируются:
- Код: `.py`, `.js`, `.ts`, `.tsx`, `.jsx`, `.java`, `.kt`, `.go`, `.rs`, `.cpp`, `.php`, `.rb`
- Конфиги: `.yaml`, `.json`, `.toml`, `.xml`
- Документация: `.md`, `.rst`, `.txt`

Исключаются:
- `node_modules/`, `venv/`, `.git/`, `__pycache__/`
- `.min.js`, `*.lock`, `dist/`, `build/`

## 🐛 Troubleshooting

### "Error 429 - insufficient_quota"

Закончились деньги на OpenAI API.

**Решение:**
1. Добавьте средства: https://platform.openai.com/account/billing
2. Проверьте лимиты: https://platform.openai.com/account/limits

### "ModuleNotFoundError: No module named 'src'"

**Решение:**
Используйте `run_server.py` вместо прямого запуска:
```bash
# ✅ Правильно
python run_server.py

# ❌ Неправильно
python -m src.server
```

### Python 3.14 проблемы с ChromaDB

ChromaDB не совместим с Python 3.14.

**Решение:**
См. [SETUP_PYTHON312.md](SETUP_PYTHON312.md)

### MCP сервер не загружается

**Проверьте:**
1. Полные абсолютные пути в конфигурации
2. Python 3.12 используется: `venv/bin/python --version`
3. Логи Claude Desktop: `tail -f ~/Library/Logs/Claude/mcp*.log`

## 📚 Дополнительная документация

- **[API_REFERENCE.md](API_REFERENCE.md)** - Полное описание всех 8 инструментов
- **[CLAUDE_DESKTOP_CONFIG.md](CLAUDE_DESKTOP_CONFIG.md)** - Детальная настройка Claude Desktop
- **[IMPLEMENTATION_SUMMARY.md](IMPLEMENTATION_SUMMARY.md)** - Технические детали реализации
- **[SETUP_PYTHON312.md](SETUP_PYTHON312.md)** - Решение проблем с Python версией
- **[QUICKSTART.md](QUICKSTART.md)** - Быстрый старт

## 🤝 Contributing

Буду рад вашим предложениям и pull requests!

## 📄 Лицензия

MIT License

## 🔗 Ссылки

- [Model Context Protocol](https://modelcontextprotocol.io)
- [OpenAI API](https://platform.openai.com/docs)
- [ChromaDB](https://www.trychroma.com/)
- [Claude Desktop](https://claude.ai/download)

---

**Версия:** 1.0.0 | **Дата:** 2026-01-28 | **Python:** 3.12+ | **API:** GPT-5.2-Codex
