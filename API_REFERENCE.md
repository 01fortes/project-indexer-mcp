# API Reference - MCP Инструменты

Полное описание всех 8 MCP инструментов Project Indexer.

## 📖 Содержание

### Основные инструменты
1. [index_project](#1-index_project) - Полная индексация проекта
2. [search_code](#2-search_code) - Семантический поиск с кодом
3. [search_files](#3-search_files) - Поиск файлов (только пути)
4. [get_project_info](#4-get_project_info) - Информация о проекте

### Управление проектами
5. [list_projects](#5-list_projects) - Список всех проектов
6. [update_files](#6-update_files) - Обновить файлы
7. [remove_files](#7-remove_files) - Удалить файлы
8. [delete_project_index](#8-delete_project_index) - Удалить проект

---

## 1. `index_project`

### Описание
Полная индексация проекта с AI-анализом. Создает векторный индекс всех файлов проекта.

### Параметры

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `project_path` | string | ✅ | Абсолютный путь к корню проекта |
| `force_reindex` | boolean | ❌ | Переиндексировать даже если уже проиндексирован (default: false) |

### Процесс индексации

1. **Анализ контекста проекта** (~10-30 сек):
   - Сканирование структуры проекта
   - Чтение конфигурационных файлов
   - AI-анализ tech stack, фреймворков, архитектуры

2. **Сканирование файлов**:
   - Обход дерева директорий
   - Применение .gitignore и фильтров
   - Определение языков и типов файлов

3. **Обработка файлов**:
   - Разбивка больших файлов на чанки
   - AI-анализ каждого файла с контекстом проекта
   - Генерация embeddings

4. **Сохранение в ChromaDB**:
   - Создание векторного индекса
   - Сохранение метаданных

### Пример использования

```
Проиндексируй проект /Users/john/projects/my-app
```

```
Проиндексируй проект ~/work/backend с переиндексацией
```

### Пример ответа

```json
{
  "status": "success",
  "project_id": "project_index_a7f3e9d2c1b4",
  "project_context": {
    "project_name": "my-app",
    "tech_stack": ["Python", "TypeScript"],
    "frameworks": ["FastAPI", "React"],
    "architecture_type": "microservices",
    "purpose": "E-commerce backend API"
  },
  "stats": {
    "total_files": 150,
    "indexed_files": 145,
    "failed_files": 5,
    "total_chunks": 220,
    "total_tokens": 450000,
    "duration_seconds": 125.3,
    "context_analysis_seconds": 15.2
  },
  "errors": [
    {"file": "legacy/old.py", "error": "Encoding error"}
  ]
}
```

### Стоимость

Для проекта из 150 файлов:
- **gpt-5.2-codex**: ~$1.65-2.80
- **gpt-4o-mini**: ~$0.15-0.25

---

## 2. `search_code`

### Описание
Семантический поиск по коду с возвратом содержимого файлов. Находит релевантные фрагменты кода по естественному запросу.

### Параметры

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `project_path` | string | ✅ | Абсолютный путь к проекту |
| `query` | string | ✅ | Естественный запрос (например, "функции для аутентификации") |
| `n_results` | integer | ❌ | Количество результатов (default: 5, max: 50) |
| `file_type` | string | ❌ | Фильтр: "code", "documentation", "config", "test" |
| `language` | string | ❌ | Фильтр по языку: "python", "javascript", "java", etc. |
| `include_code` | boolean | ❌ | Включать код в результаты (default: true) |

### Пример использования

```
Найди где обрабатываются HTTP запросы
```

```
Покажи функции для работы с базой данных
```

```
Найди тесты для authentication на Python
```

### Пример ответа

```json
{
  "status": "success",
  "results": [
    {
      "file_path": "/project/src/api/routes.py",
      "relative_path": "src/api/routes.py",
      "chunk_index": 0,
      "score": 0.89,
      "purpose": "Defines HTTP API routes and handlers for the application",
      "dependencies": ["fastapi", "pydantic"],
      "exported_symbols": ["router", "get_user", "create_user"],
      "code": "from fastapi import APIRouter\n\nrouter = APIRouter()\n\n@router.get('/users')\nasync def get_user():\n    ...",
      "metadata": {
        "language": "python",
        "file_type": "code"
      }
    },
    {
      "file_path": "/project/src/api/middleware.py",
      "relative_path": "src/api/middleware.py",
      "chunk_index": 0,
      "score": 0.85,
      "purpose": "HTTP middleware for request processing",
      "dependencies": ["starlette"],
      "exported_symbols": ["AuthMiddleware"],
      "code": "class AuthMiddleware:\n    async def __call__(self, request):\n        ...",
      "metadata": {
        "language": "python",
        "file_type": "code"
      }
    }
  ],
  "query_time_ms": 45
}
```

---

## 3. `search_files`

### Описание
Быстрый поиск файлов по семантическому запросу. Возвращает только пути файлов без кода - быстрее и компактнее чем `search_code`.

### Параметры

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `project_path` | string | ✅ | Абсолютный путь к проекту |
| `query` | string | ✅ | Естественный запрос |
| `n_results` | integer | ❌ | Количество уникальных файлов (default: 10) |

### Пример использования

```
Найди файлы связанные с аутентификацией
```

```
Покажи все файлы где есть работа с PostgreSQL
```

### Пример ответа

```json
{
  "status": "success",
  "query": "authentication logic",
  "total_files": 5,
  "files": [
    {
      "file_path": "/project/src/auth/login.py",
      "relative_path": "src/auth/login.py",
      "score": 0.89,
      "purpose": "Handles user authentication and JWT token generation",
      "language": "python"
    },
    {
      "file_path": "/project/src/auth/middleware.py",
      "relative_path": "src/auth/middleware.py",
      "score": 0.85,
      "purpose": "Authentication middleware for API routes",
      "language": "python"
    }
  ]
}
```

### Отличие от search_code

| Критерий | search_files | search_code |
|----------|--------------|-------------|
| Возвращает код | ❌ | ✅ |
| Дедупликация | ✅ (уникальные файлы) | ❌ (может вернуть чанки одного файла) |
| Скорость | ⚡ Быстрее | 🐢 Медленнее |
| Размер ответа | 📦 Компактный | 📚 Большой |
| Использование | "Где искать?" | "Покажи код" |

---

## 4. `get_project_info`

### Описание
Получить информацию о проиндексированном проекте: контекст, статистику, состояние.

### Параметры

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `project_path` | string | ✅ | Абсолютный путь к проекту |

### Пример использования

```
Покажи информацию о проекте ~/work/backend
```

```
Статистика индексации для /Users/john/my-app
```

### Пример ответа

```json
{
  "status": "indexed",
  "project_id": "project_index_a7f3e9d2c1b4",
  "project_context": {
    "project_name": "my-api",
    "project_description": "RESTful API for e-commerce platform",
    "tech_stack": ["Python", "FastAPI"],
    "frameworks": ["FastAPI", "SQLAlchemy", "Pydantic"],
    "architecture_type": "microservices",
    "purpose": "Backend API for product catalog and orders"
  },
  "indexed_at": 1706472000.0,
  "stats": {
    "total_documents": 221,
    "languages": {
      "python": 80,
      "typescript": 45,
      "markdown": 10
    }
  }
}
```

---

## 5. `list_projects`

### Описание
Показать все проиндексированные проекты в ChromaDB. Полезно для обзора всех доступных проектов.

### Параметры

Нет параметров.

### Пример использования

```
Покажи все проиндексированные проекты
```

```
Какие проекты у меня в индексе?
```

### Пример ответа

```json
{
  "status": "success",
  "total_projects": 3,
  "projects": [
    {
      "project_name": "my-api",
      "project_path": "/Users/john/projects/my-api",
      "tech_stack": ["Python", "FastAPI"],
      "frameworks": ["FastAPI", "SQLAlchemy"],
      "architecture_type": "microservices",
      "total_files": 145,
      "collection_name": "project_index_a7f3e9d2c1b4",
      "indexed_at": 1706472000.0
    },
    {
      "project_name": "frontend-app",
      "project_path": "/Users/john/projects/frontend",
      "tech_stack": ["TypeScript", "React"],
      "frameworks": ["React", "Next.js"],
      "architecture_type": "web-app",
      "total_files": 87,
      "collection_name": "project_index_b8c5d6e3a2f1",
      "indexed_at": 1706468000.0
    }
  ]
}
```

---

## 6. `update_files`

### Описание
Обновить или добавить конкретные файлы или целые директории в индекс без переиндексации всего проекта. Полезно при изменении нескольких файлов или папок.

### Параметры

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `project_path` | string | ✅ | Абсолютный путь к проекту |
| `file_paths` | array[string] | ✅ | Список относительных путей файлов **или директорий** |

**Важно:** Можно указывать как файлы, так и папки:
- `"src/main.py"` - обновит один файл
- `"src/api/"` - обновит все файлы в папке `src/api/` рекурсивно
- `["src/api/", "tests/unit/", "config.py"]` - обновит две папки и один файл

### Процесс обновления

1. Удаляет старые версии указанных файлов из индекса
2. Заново анализирует файлы с контекстом проекта
3. Генерирует новые embeddings
4. Сохраняет в ChromaDB

### Пример использования

**Обновление файлов:**
```
Обнови файлы src/main.py и src/config.py в проекте ~/my-app
```

**Обновление целой папки:**
```
Обнови папку src/api/ в проекте ~/my-app
```

```
Переиндексируй директорию tests/unit/
```

**Комбинированное:**
```
Обнови src/api/, tests/ и файл README.md в проекте ~/my-app
```

### Пример ответа

```json
{
  "status": "success",
  "stats": {
    "updated_files": 2,
    "failed_files": 0,
    "total_chunks": 3
  },
  "errors": []
}
```

### Use Cases

- ✅ Изменили файл - обновить в индексе
- ✅ Добавили новый файл - добавить в индекс
- ✅ Рефакторинг нескольких файлов - обновить их
- ✅ Исправили баг - переиндексировать файл
- ✅ Переписали целый модуль - обновить всю папку `src/auth/`
- ✅ Добавили новую фичу в `features/payments/` - проиндексировать папку
- ✅ Обновили тесты в `tests/api/` - переиндексировать директорию

---

## 7. `remove_files`

### Описание
Удалить конкретные файлы или целые директории из индекса. Используется когда файлы/папки удалены из проекта или больше не нужны в поиске.

### Параметры

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `project_path` | string | ✅ | Абсолютный путь к проекту |
| `file_paths` | array[string] | ✅ | Список относительных путей файлов **или директорий** |

**Важно:** Можно указывать как файлы, так и папки:
- `"old/deprecated.py"` - удалит один файл
- `"legacy/"` - удалит все файлы в папке `legacy/` рекурсивно
- `["old/", "temp/cache.py"]` - удалит папку и файл

### Пример использования

**Удаление файлов:**
```
Удали из индекса файл old/deprecated.py
```

**Удаление целой папки:**
```
Удали из индекса папку legacy/
```

```
Убери директорию temp/ из индекса
```

**Комбинированное:**
```
Удали old/, temp/ и файл cache.json
```

### Пример ответа

```json
{
  "status": "success",
  "removed_paths": 2,
  "removed_files": 15,
  "removed_chunks": 23
}
```

Где:
- `removed_paths` - количество указанных путей (файлов/папок)
- `removed_files` - общее количество удаленных файлов
- `removed_chunks` - количество удаленных чанков из ChromaDB

### Use Cases

- ✅ Удалили файл из проекта
- ✅ Файл устарел и мешает в поиске
- ✅ Переместили файл (удалить старый путь, добавить новый)
- ✅ Временные файлы попали в индекс
- ✅ Удалили целый модуль/папку из проекта
- ✅ Очистили старую директорию `old/` или `deprecated/`
- ✅ Убрали тестовые файлы `temp/` или `scratch/`

---

## 8. `delete_project_index`

### Описание
Полностью удалить индекс проекта из ChromaDB. **Необратимая операция!**

### Параметры

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `project_path` | string | ✅ | Абсолютный путь к проекту |
| `confirm` | boolean | ✅ | Должен быть `true` для подтверждения |

### Пример использования

```
Удали индекс проекта ~/old-project с подтверждением
```

### Пример ответа

```json
{
  "status": "success",
  "message": "Project index deleted for /Users/john/old-project"
}
```

### ⚠️ Предупреждение

- Удаляются ВСЕ данные проекта из ChromaDB
- Удаляется контекст проекта
- Удаляются все embeddings
- Операция необратима
- Требуется `confirm=true`

---

## 💡 Частые сценарии использования

### Сценарий 1: Первая индексация
```
1. index_project - Проиндексировать весь проект
2. search_code - Найти нужный код
3. get_project_info - Проверить статистику
```

### Сценарий 2: Обновление после изменений
```
1. update_files - Обновить измененные файлы
2. search_code - Найти обновленный код
```

### Сценарий 3: Удаление старых файлов
```
1. remove_files - Удалить ненужные файлы
2. list_projects - Проверить состояние
```

### Сценарий 4: Быстрый поиск файлов
```
1. search_files - Найти файлы по запросу
2. search_code - Посмотреть код в найденных файлах
```

### Сценарий 5: Очистка
```
1. list_projects - Посмотреть все проекты
2. delete_project_index - Удалить ненужные
```

---

## 🔍 Советы по использованию

### Формулировка запросов

**✅ Хорошие запросы:**
- "функции для аутентификации пользователей"
- "где обрабатываются HTTP запросы"
- "код для работы с PostgreSQL"
- "тесты для API endpoints"
- "конфигурация Docker"

**❌ Плохие запросы:**
- "auth" (слишком короткий)
- "функция" (слишком общий)
- "код" (нет контекста)

### Выбор между search_code и search_files

Используйте **search_files** когда:
- Нужно только найти где искать
- Хотите быстрый ответ
- Работаете с большим проектом
- Нужен обзор файлов

Используйте **search_code** когда:
- Нужно увидеть реальный код
- Хотите понять реализацию
- Нужны детали и контекст
- Планируете копировать код

### Оптимизация индексации

**Для больших проектов:**
1. Используйте `force_reindex=false` при повторной индексации
2. Обновляйте только измененные файлы через `update_files`
3. Удаляйте ненужные файлы через `remove_files`

**Для экономии:**
- Индексируйте только нужные директории
- Исключайте тесты если они не нужны
- Используйте `.gitignore` для автоматической фильтрации

---

## 📊 Производительность

| Операция | Проект 50 файлов | Проект 200 файлов | Проект 1000 файлов |
|----------|------------------|-------------------|---------------------|
| index_project | ~30 сек | ~2 мин | ~10 мин |
| search_code | ~0.5 сек | ~1 сек | ~2 сек |
| search_files | ~0.3 сек | ~0.5 сек | ~1 сек |
| update_files (5) | ~10 сек | ~10 сек | ~10 сек |

*Время приблизительное, зависит от размера файлов и скорости OpenAI API*

---

**Версия:** 1.0.0 | **Дата:** 2026-01-28
