# MCP Server for Project Indexing
# Install: pip install -r requirements.txt

# Core MCP
mcp>=1.0.0

# Vector DB - full version with persistent storage support
# Note: chromadb-client is HTTP-only and doesn't support PersistentClient
chromadb>=0.5.0

# OpenAI
openai>=1.0.0

# File processing
python-dotenv>=1.0.0
pathspec>=0.11.0
aiofiles>=23.0.0
tiktoken>=0.5.0
pyyaml>=6.0

# Data validation (flexible version)
pydantic>=1.9,<3.0

# Web server (for admin portal)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# AST parsing for call graph
# Note: tree-sitter 0.22+ breaks compatibility with tree-sitter-languages 1.10.x
tree-sitter>=0.21.0,<0.22
tree-sitter-languages>=1.10.0
