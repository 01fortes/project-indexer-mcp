# 🌐 Web Admin Portal

Веб-интерфейс для управления и просмотра индексированных проектов.

## 🚀 Запуск

### Быстрый старт

```bash
# Запустить на порту 8080 (по умолчанию)
python run_web_server.py

# Запустить на другом порту
python run_web_server.py --port 3000

# Запустить на определенном хосте
python run_web_server.py --host 127.0.0.1 --port 8080
```

После запуска откройте в браузере:
```
http://localhost:8080
```

## 📊 Возможности

### Веб-интерфейс
- ✅ Просмотр всех индексированных проектов
- ✅ Статистика (количество проектов, файлов)
- ✅ Детальная информация о каждом проекте
- ✅ Tech stack и frameworks
- ✅ Архитектура проекта
- ✅ Дата индексации
- ✅ **Список всех файлов проекта**
- ✅ **Поиск по имени файла**
- ✅ **Информация о файлах:** язык, тип, назначение, количество чанков
- ✅ **Цветовое кодирование** типов файлов
- ✅ **Просмотр содержимого файлов** - клик на файл показывает все чанки
- ✅ **Метаданные чанков:** dependencies, exported symbols, purpose
- ✅ **Копирование кода** в буфер обмена
- ✅ **Темная тема** для отображения кода
- ✅ **Удаление проекта** из индекса с подтверждением (NEW!)
- ✅ **Обновление индекса файла** - пере-индексация отдельных файлов (NEW!)
- ✅ **Система уведомлений** - success/error/info toast messages (NEW!)

### REST API Endpoints

#### 1. Health Check
```bash
GET /api/health
```
Проверка работоспособности сервера.

**Ответ:**
```json
{
  "status": "healthy",
  "version": "1.0.0",
  "mcp_server": "project-indexer"
}
```

#### 2. Список проектов
```bash
GET /api/projects
```
Получить список всех проектов.

**Ответ:**
```json
{
  "total": 2,
  "projects": [
    {
      "project_name": "My Project",
      "project_path": "/path/to/project",
      "tech_stack": ["Python", "JavaScript"],
      "frameworks": ["FastAPI", "React"],
      "architecture_type": "microservices",
      "total_files": 150,
      "collection_name": "project_abc123",
      "indexed_at": 1738329600
    }
  ]
}
```

#### 3. Информация о проекте
```bash
GET /api/projects/{project_path}/info
```
Детальная информация о проекте.

**Пример:**
```bash
curl "http://localhost:8080/api/projects/%2Fpath%2Fto%2Fproject/info"
```

#### 4. Список файлов проекта
```bash
GET /api/projects/{project_path}/files?limit=100&offset=0
```
Получить список файлов с пагинацией.

**Параметры:**
- `limit` - количество результатов (по умолчанию 100)
- `offset` - смещение для пагинации (по умолчанию 0)

#### 5. Поиск в проекте
```bash
GET /api/projects/{project_path}/search?query=authentication+logic&n_results=10&file_type=code&language=python
```
Семантический поиск по коду.

**Параметры (query string):**
- `query` - поисковый запрос (обязательный)
- `n_results` - количество результатов (по умолчанию 10)
- `file_type` - тип файла (optional): code, documentation, config, test
- `language` - язык программирования (optional)

#### 6. Получить содержимое файла (NEW!)
```bash
GET /api/projects/{project_path}/files/{file_path}
```
Получить все чанки конкретного файла с метаданными.

**Пример:**
```bash
curl "http://localhost:8080/api/projects/%2Fpath%2Fto%2Fproject/files/src%2Fmain.py"
```

**Ответ:**
```json
{
  "status": "success",
  "file_path": "src/main.py",
  "total_chunks": 2,
  "chunks": [
    {
      "chunk_index": 0,
      "total_chunks": 2,
      "content": "import os\n\ndef main():\n    ...",
      "purpose": "Main application entry point",
      "dependencies": ["os", "sys"],
      "exported_symbols": ["main"],
      "language": "python",
      "file_type": "code"
    }
  ]
}
```

#### 7. Обновить индекс файлов
```bash
POST /api/projects/{project_path}/files/update
Content-Type: application/json

{
  "file_paths": ["src/main.py", "src/utils.py"]
}
```
Обновить индекс конкретных файлов в проекте.

**Request body:**
- `file_paths` - список относительных путей файлов для обновления (обязательный)

**Response:**
```json
{
  "status": "success",
  "stats": {
    "updated_files": 2,
    "failed_files": 0,
    "total_chunks": 5
  }
}
```

#### 8. Удалить проект
```bash
DELETE /api/projects/{project_path}
```
Удалить проект из индекса.

## 🔧 Конфигурация

В файле `.env`:

```bash
# Web Server Configuration
WEB_HOST=0.0.0.0        # Адрес хоста
WEB_PORT=8080           # Порт
WEB_ENABLE=false        # Автозапуск (пока не реализовано)
```

## 📚 Документация API

После запуска сервера доступна автоматическая документация:

- **Swagger UI**: http://localhost:8080/docs
- **ReDoc**: http://localhost:8080/redoc
- **OpenAPI JSON**: http://localhost:8080/openapi.json

## 🎨 Технологии

**Backend:**
- FastAPI 0.128.0
- Uvicorn (ASGI сервер)
- Переиспользование IndexManager и ChromaManager

**Frontend:**
- HTML5 + Vue.js 3 (через CDN)
- Tailwind CSS (через CDN)
- Vanilla JavaScript

## 🔒 Безопасность

⚠️ **Важно:**
- Веб-портал не имеет аутентификации
- Рекомендуется использовать только в локальной сети
- Для production нужно добавить:
  - Аутентификацию (OAuth, JWT)
  - HTTPS
  - Rate limiting
  - CORS ограничения

## 🐛 Отладка

### Проверка работы сервера
```bash
curl http://localhost:8080/api/health
```

### Просмотр логов
Логи выводятся в консоль при запуске:
```
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8080
```

### Проверка доступных проектов
```bash
curl http://localhost:8080/api/projects | jq
```

## 📝 Примеры использования

### Поиск функций аутентификации
```bash
curl "http://localhost:8080/api/projects/%2Fpath%2Fto%2Fproject/search?query=authentication+functions&n_results=5&file_type=code" | jq
```

### Получить список Python файлов
```bash
curl "http://localhost:8080/api/projects/%2Fpath%2Fto%2Fproject/files?limit=50" | jq
```

## 🚀 Развитие

Планируемые улучшения:
- [ ] Аутентификация и авторизация
- [ ] Инкрементальная индексация через UI
- [ ] Графики и визуализация статистики
- [ ] Экспорт результатов поиска
- [ ] Темная тема
- [ ] WebSocket для real-time обновлений
- [ ] Сравнение проектов
- [ ] История изменений

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи сервера
2. Убедитесь что ChromaDB инициализирован
3. Проверьте что порт 8080 не занят
4. Проверьте файрвол и сетевые настройки
